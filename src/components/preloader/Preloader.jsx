import { PreloaderContainer, PreloaderIcon } from './preloader.styles';

const Preloader = () => {
	return (
		<PreloaderContainer>
			<PreloaderIcon />
		</PreloaderContainer>
	);
};

export default Preloader;
